---
title: 确认 Base URL
description: 一句话规则是只换域名不换路径。这页说清站点地址、带 /v1 的 Base URL 和完整接口地址三者分别填在哪。
---

API Key 回答"你是谁"，Base URL 回答"请求发到哪"。两个是不同的东西，别填串了。

## 一句话规则：只换域名，不换路径

接入 Pixel API 不需要改接口格式，只需要把官方域名换成站点域名。

官方 OpenAI 的 Chat Completions 是：

```text
https://api.openai.com/v1/chat/completions
```

换成 Pixel API 就是：

```text
https://ai-pixel.online/v1/chat/completions
```

域名 `api.openai.com` → `ai-pixel.online`，后面的 `/v1/chat/completions` 一个字都不改。记住这条，大部分客户端都能自己推出来该怎么填。

## `/v1` 到底要不要带

看客户端问你要什么：

| 客户端的字段 | 你填 | 例子 |
| --- | --- | --- |
| Base URL / API Base / Server URL / Provider URL | 站点地址，不带 `/v1` | `https://ai-pixel.online` |
| OpenAI SDK 风格的 `base_url`（如 Codex） | 带 `/v1` | `https://ai-pixel.online/v1` |
| Endpoint / 接口地址（要完整的） | 带完整路径 | `https://ai-pixel.online/v1/chat/completions` |

常用的完整地址就三个：Chat Completions 是 `/v1/chat/completions`，Responses 是 `/v1/responses`，列模型是 `/v1/models`。

## 不确定就照抄弹窗

上一页那个「使用密钥」弹窗里给的配置是按你这个 Key 的实际分组生成的，**照抄它永远对**。文档里的示例只是示例——如果你的控制台显示的是另一个域名，以控制台为准。

下一步：[发送第一条消息](/docs/normal-send-test-message)。
